# ECN-037: validate_layoutがネスト内のブロック横切りを検出できない

## 不具合

validate_layoutのルート横切り検出（チェック#6）はroot_states（parent=None）のみチェック。
diagnostic→h_runの遷移ルートがcruise（activeの子、3階層目）を横切るが検出されない。

## 原因

```python
for s in root_states:  # ← parent=Noneのみ
```

## 対策

遷移の両端点の共通祖先を特定し、その祖先の全子孫ステートに対してルート横切りをチェックする。
