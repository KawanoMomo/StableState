# ECN-032: コンポジットステート選択中の要素追加

## 不具合

コンポジットステート（例: Active）を選択した状態で+State/●Initial/◎Finalボタンを押すと、ルートレベルの固定座標に配置される。コンポジットの中に追加されない。

## 対策

選択中のステートがコンポジット（children > 0）の場合:
- 新規要素をDSLのコンポジットの`{ }`ブロック内に挿入
- 座標を親の相対座標に設定（initial: 左端、final: 右端、state: 内部）
- group/noteは対象外（ルートレベルに追加）
