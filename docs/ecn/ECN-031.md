# ECN-031: 擬似状態の配置精度

## 不具合

擬似状態(initial, final等)はグリッド中心座標で描画されるが、ドラッグ時は1グリッド単位でスナップされる。ステートの縦中心は `y + h/2` でグリッドの半端な位置になるため、擬似状態を横に並べても矢印が水平にならない。

## 対策

擬似状態のドラッグを0.5グリッド単位にスナップ。DSL座標で小数を許容（`at 1,5.5`）。
