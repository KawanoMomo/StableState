# ISSUE-005: 矢印キー移動・接続管理パネルが未実装

- **ステータス**: Open
- **種別**: 機能追加
- **優先度**: MEDIUM
- **発見日**: 2026-03-28
- **影響ファイル**:
  - `stablestate.html`
  - `vscode-stablestate/src/webview.html`

## コンテキスト

StableBlockで実装済みの「矢印キーによる1グリッド単位の要素移動」と「2要素選択時の接続管理パネル（削除/反転/双方向化）」がStableStateでは未実装。

## 問題の詳細

**矢印キー移動（未実装）:**
- `keydown` ハンドラに Arrow Up/Down/Left/Right の処理がない
- 現状は選択要素をドラッグでしか移動できない
- グリッド単位の精密配置に不便

**接続管理パネル（部分実装）:**
2要素選択時のプロパティパネルに「Connect」ボタンのみ存在 (`stablestate.html:2092-2095`)。以下が未実装:
- 既存接続の一覧表示
- 接続削除
- 遷移方向の反転
- 双方向遷移への切替
- グループ内ステート追加

## 対策

（未記入 — 着手時に記載）

## 結果

（未記入 — 完了時に記載）
