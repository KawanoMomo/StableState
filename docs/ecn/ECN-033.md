# ECN-033: コンポジットステート内の擬似状態が選択できない + 選択表示なし

## 不具合

1. hitTestがステート→擬似状態の順で判定するため、コンポジットステート内の擬似状態をクリックすると親ステートがヒットする
2. 擬似状態は選択されても見た目が変わらないため、選択状態が分からない

## 対策

1. hitTestの優先順を擬似状態→ステート→グループに変更
2. 選択中の擬似状態に白い破線リングを表示（initial/final）、stroke色を白に変更（choice/history/fork/join）
